<h1><%= @band.name %></h1>

<ul>
<% @band.albums. each do |album| %>
  <li><%= link_to album.title, album_path(album) %></li>
<% end %>
</ul>
<br>
<div><%= link_to "Edit #{@band.name}", edit_band_path(@band) %></div>
<div><%= link_to "See all bands", bands_path %></div>